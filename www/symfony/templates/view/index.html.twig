{% extends 'base.html.twig' %}

{% block body %}
<h1>Hello Udemy!</h1>

    {{ d }}

    <h1>{{ user.alter }}</h1>

    <ul>
        {% for data in l %}
            <li>{{ data }}</li>
        {% endfor %}
    </ul>

{% endblock %}
